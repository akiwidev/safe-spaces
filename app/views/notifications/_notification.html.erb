<%= image_tag(current_user.photo, alt: "profile photo") %>
<%# <%= link_to image_tag(notification.params[:space].user.photo, alt: "profile photo"), user_path(notification.params[:space].user) %>
<%# <%= link_to cl_image_tag(notification.incident.user.photo.key, alt: "profile photo"), user_path(notification.incident.user) %>
